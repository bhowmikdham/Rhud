<?xml version="1.0" encoding="UTF-8"?>
<!--
  DEV variant of manifest.xml. Identical to the production manifest
  except every URL points at https://localhost:4000 (the Vite dev
  server) instead of https://addin.rhud.net.

  Workflow:
    1. `pnpm addin:dev` to start Vite at https://localhost:4000
    2. Visit https://localhost:4000 in your browser once and accept
       the self-signed cert (otherwise Outlook silently refuses to
       load the task pane and you get a blank pane with no error).
    3. Sideload THIS manifest (manifest.dev.xml), not manifest.xml.
    4. When you make code changes, the Vite HMR refreshes the pane
       on next open — no need to re-sideload unless you edit this
       manifest itself.

  The <Id> is the same dev GUID used in manifest.xml so Outlook treats
  them as the same add-in (no double-install). If you ever want to run
  prod + dev side-by-side in the same mailbox, generate a fresh GUID
  for one of them.
-->
<OfficeApp
    xmlns="http://schemas.microsoft.com/office/appforoffice/1.1"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:bt="http://schemas.microsoft.com/office/officeappbasictypes/1.0"
    xmlns:mailappor="http://schemas.microsoft.com/office/mailappversionoverrides/1.0"
    xsi:type="MailApp">

  <Id>50da4d3a-2967-48ed-8669-2c42acbb35c7</Id>
  <Version>1.0.0.0</Version>
  <ProviderName>Rhud</ProviderName>
  <DefaultLocale>en-US</DefaultLocale>
  <DisplayName DefaultValue="Rhud (dev)" />
  <Description DefaultValue="Turn an inbound email into a Rhud opportunity in one click. (dev build)" />
  <IconUrl DefaultValue="https://localhost:4000/icons/icon-64.png" />
  <HighResolutionIconUrl DefaultValue="https://localhost:4000/icons/icon-128.png" />
  <SupportUrl DefaultValue="https://rhud.net/support" />

  <AppDomains>
    <AppDomain>https://rhud.net</AppDomain>
    <AppDomain>https://localhost:4000</AppDomain>
  </AppDomains>

  <Hosts>
    <Host Name="Mailbox" />
  </Hosts>

  <Requirements>
    <Sets>
      <Set Name="Mailbox" MinVersion="1.5" />
    </Sets>
  </Requirements>

  <FormSettings>
    <Form xsi:type="ItemRead">
      <DesktopSettings>
        <SourceLocation DefaultValue="https://localhost:4000/index.html" />
        <RequestedHeight>450</RequestedHeight>
      </DesktopSettings>
    </Form>
  </FormSettings>

  <Permissions>ReadWriteMailbox</Permissions>

  <Rule xsi:type="RuleCollection" Mode="Or">
    <Rule xsi:type="ItemIs" ItemType="Message" FormType="Read" />
  </Rule>

  <DisableEntityHighlighting>false</DisableEntityHighlighting>

  <VersionOverrides
      xmlns="http://schemas.microsoft.com/office/mailappversionoverrides"
      xsi:type="VersionOverridesV1_0">
    <Requirements>
      <bt:Sets DefaultMinVersion="1.5">
        <bt:Set Name="Mailbox" />
      </bt:Sets>
    </Requirements>
    <Hosts>
      <Host xsi:type="MailHost">
        <DesktopFormFactor>
          <FunctionFile resid="FunctionFileUrl" />
          <ExtensionPoint xsi:type="MessageReadCommandSurface">
            <OfficeTab id="TabDefault">
              <Group id="rhudGroup">
                <Label resid="GroupLabel" />
                <Control xsi:type="Button" id="createOpportunityBtn">
                  <Label resid="BtnLabel" />
                  <Supertip>
                    <Title resid="BtnLabel" />
                    <Description resid="BtnDesc" />
                  </Supertip>
                  <Icon>
                    <bt:Image size="16" resid="Icon16" />
                    <bt:Image size="32" resid="Icon32" />
                    <bt:Image size="80" resid="Icon80" />
                  </Icon>
                  <Action xsi:type="ShowTaskpane">
                    <SourceLocation resid="TaskpaneUrl" />
                  </Action>
                </Control>
              </Group>
            </OfficeTab>
          </ExtensionPoint>
        </DesktopFormFactor>
      </Host>
    </Hosts>
    <Resources>
      <bt:Images>
        <bt:Image id="Icon16" DefaultValue="https://localhost:4000/icons/icon-16.png" />
        <bt:Image id="Icon32" DefaultValue="https://localhost:4000/icons/icon-32.png" />
        <bt:Image id="Icon80" DefaultValue="https://localhost:4000/icons/icon-80.png" />
      </bt:Images>
      <bt:Urls>
        <bt:Url id="TaskpaneUrl" DefaultValue="https://localhost:4000/index.html" />
        <bt:Url id="FunctionFileUrl" DefaultValue="https://localhost:4000/index.html" />
      </bt:Urls>
      <bt:ShortStrings>
        <bt:String id="GroupLabel" DefaultValue="Rhud" />
        <bt:String id="BtnLabel" DefaultValue="Create Opportunity" />
      </bt:ShortStrings>
      <bt:LongStrings>
        <bt:String id="BtnDesc" DefaultValue="Create a Rhud opportunity from this email. (dev)" />
      </bt:LongStrings>
    </Resources>
  </VersionOverrides>
</OfficeApp>
